<Window x:Class="PrintService.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrintService"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Button Content="测试打开web服务" Width="200" Height="100" Command="{Binding ViewModel.OpenServer}" />
        <Button Content="初始化数据库" Width="200" Height="100" Command="{Binding ViewModel.InitDatabase}" Margin="299,43,301,292" />
    </Grid>
</Window>
